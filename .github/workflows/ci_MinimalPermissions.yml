name: DBA Hawk CI with Minimal Permissions

on: 
    push:
    workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: windows-latest

    strategy:
      fail-fast: false
      matrix:
        language: [csharp]
        sql-version: [2022]
        sql-collation: [Latin1_General_BIN,SQL_1xCompat_CP850_CI_AS]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
          dotnet-quality: 'ga'
      
      - name: Check SDK version
        run: dotnet --list-sdks

      - name: Build
        run: dotnet build -c CLI
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build GUI
        run: dotnet build DBAHawkGUI -o DBADashBuild\DBADashGUIOnly

      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v3
        
      - name: Build DB
        run: msbuild dbadashdb -property:Configuration=CLI

      - name: Get Version
        id: GetVersion 
        shell: powershell
        run: | 
          $path = [System.IO.Path]::Combine((Get-Location),"DBADashBuild\CLI\DBAHawk.dll")
          $version = [System.Reflection.Assembly]::LoadFrom($path).GetName().Version
          $version.ToString(3)
          Write-Output "BUILD_NUMBER=$($version.ToString(3))" >> $env:GITHUB_OUTPUT

      - name: Get Build Reference
        id: GetBuildReference
        shell: powershell
        run: | 
                Invoke-WebRequest -Uri 'https://dataplat.dbatools.io/assets/dbatools-buildref-index.json' -OutFile 'DBADashBuild\CLI\BuildReference.json'

      - name: Zip
        shell: powershell
        run: | 
          $zipPath = "DBADash_${{steps.GetVersion.outputs.BUILD_NUMBER}}.zip"
          Compress-Archive -Path "DBADashBuild\CLI\*" -DestinationPath $zipPath
          $guiZipPath = "DBADash_GUI_Only_${{steps.GetVersion.outputs.BUILD_NUMBER}}.zip"
          Compress-Archive -Path "DBADashBuild\DBADashGUIOnly\*" -DestinationPath $guiZipPath

      - name: Install SQL
        uses: potatoqualitee/mssqlsuite@v1.12
        with:
          install: sqlengine
          version: ${{ matrix.sql-version }}
          collation: ${{ matrix.sql-collation }}

      - name: Check SQL Install
        run: | 
          sqlcmd -S localhost -U sa -P dbatools.I0 -d tempdb -Q "SELECT @@version as Version;"
          sqlcmd -S localhost -U sa -P dbatools.I0 -d tempdb -Q "SELECT SERVERPROPERTY('Collation') AS Collation;"

      - name: Create Identity Test DB
        run: | 
          sqlcmd -S localhost -U sa -P dbatools.I0 -d tempdb -Q "CREATE DATABASE TestIdentity"
          sqlcmd -S localhost -U sa -P dbatools.I0 -d TestIdentity -Q "CREATE TABLE dbo.IdentityTest(ID TINYINT IDENTITY(1,1));DECLARE @i INT=1; WHILE @i <250 BEGIN INSERT INTO dbo.IdentityTest  DEFAULT VALUES; SET @i+=1; END"

      - name: Add SQL Login for DBA Hawk service with minimal permissions
        run: | 
          sqlcmd -S localhost -U sa -P dbatools.I0 -d master -Q "CREATE LOGIN [DBAHawkService] WITH PASSWORD=N'Use_A_Str0ng_Passw0rd', DEFAULT_DATABASE=[master], CHECK_EXPIRATION=OFF, CHECK_POLICY=OFF
          GRANT CREATE ANY DATABASE TO [DBAHawkService] /* Only required to allow service account to create repository database */
          GRANT VIEW SERVER STATE TO [DBAHawkService]
          GRANT VIEW ANY DATABASE TO [DBAHawkService]
          GRANT CONNECT ANY DATABASE TO [DBAHawkService]
          GRANT VIEW ANY DEFINITION TO [DBAHawkService]
          GRANT ALTER ANY EVENT SESSION TO [DBAHawkService] /* Required if you want to use slow query capture */
          USE [msdb]
          IF NOT EXISTS(SELECT * 
                FROM msdb.sys.database_principals
                WHERE name = 'DBAHawkService')
          BEGIN
            CREATE USER [DBAHawkService] FOR LOGIN [DBAHawkService]
          END
          ALTER ROLE [db_datareader] ADD MEMBER [DBAHawkService]
          ALTER ROLE [SQLAgentReaderRole] ADD MEMBER [DBAHawkService]"

      - name: Configure & Install DBA Hawk as Service
        shell: powershell
        run: | 
          $ErrorActionPreference = "Stop" 
          $zipPath = "DBADash_${{steps.GetVersion.outputs.BUILD_NUMBER}}.zip"
          $InstallPath = "C:\DBADashTest"
          "Extract to $InstallPath"
          Expand-Archive -Path $zipPath -DestinationPath $InstallPath -Force -ErrorAction Stop
          Set-Location $InstallPath
          "Configure"
          ./dbahawkconfig -c "Data Source=localhost;UID=DBAHawkService;pwd=Use_A_Str0ng_Passw0rd;Initial Catalog=DBADashDB_GitHubAction;Encrypt=True;TrustServerCertificate=True;" -a SetDestination
          ./dbahawkconfig -c "Data Source=localhost;UID=DBAHawkService;pwd=Use_A_Str0ng_Passw0rd;Encrypt=True;TrustServerCertificate=True;" -a Add --PlanCollectionEnabled --SlowQueryThresholdMs 1000 --SchemaSnapshotDBs "*" --NoWMI
          "Install Service"
          ./DBAHawkService install --localservice
          "Start Service"
          net start DBAHawkService
          "Wait 60 sec"
          Start-Sleep -s 60

          "Get Logs"
          $logsFolder = "$InstallPath\Logs"

          Get-ChildItem -Path $logsFolder | Get-Content

          exit 0

      - name: Wait 5min
        shell: powershell
        run: | 
          "Wait 5min"
          Start-Sleep -s 300
      - name: Stop services
        run: |
          net stop DBAHawkService
      - name: Output CollectionErrorLog
        shell: powershell
        run: | 
          Invoke-Sqlcmd -ServerInstance $params.ServerInstance -Database "DBADashDB_GitHubAction" -Query "SELECT * FROM dbo.CollectionErrorLog" -TrustServerCertificate | Format-Table

      - name: Output Table Counts
        shell: powershell
        run: | 
          ./Scripts/Get-TableCounts -ServerInstance "LOCALHOST" -Database "DBADashDB_GitHubAction" 

      - name: Output Log and Check for Errors
        shell: powershell
        run: | 
          ./Scripts/Get-LogContent -LogPath  "C:\DBADashTest\Logs" -ThrowError

      - name: Run Pester Tests
        shell: pwsh
        run: |
          # pwsh ships Pester 5.x preinstalled; only install if it is missing
          if (-not (Get-Module -ListAvailable -Name Pester | Where-Object { $_.Version -ge [version]'5.0.0' })) {
            Install-Module Pester -Force -SkipPublisherCheck -Scope CurrentUser -MinimumVersion 5.0.0
          }
          Import-Module Pester -MinimumVersion 5.0.0
          $NoWMI=$true
          Invoke-Pester -Output Detailed Scripts\CI_Workflow.Tests.ps1
